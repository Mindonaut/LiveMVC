define(function ( require ) {
  var HttpServerView = require("HttpServerView");
  var HttpServerController = require("HttpServerController");
  var Model = require("Model");
  
  
  var movable = Object.defineProperties({
    startView: {
      value: function() {}
    },
    startController: {
      
    },
    startModel: {
      
    }
    
    
    
  });



});